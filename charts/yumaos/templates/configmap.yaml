apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "yumaos.fullname" . }}-config
  labels:
    {{- include "yumaos.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.env }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
