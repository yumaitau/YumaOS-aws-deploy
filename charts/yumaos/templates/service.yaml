apiVersion: v1
kind: Service
metadata:
  name: {{ include "yumaos.fullname" . }}-web
  labels:
    {{- include "yumaos.labels" . | nindent 4 }}
    app.kubernetes.io/component: web
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "yumaos.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: app
