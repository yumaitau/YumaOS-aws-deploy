compact: true
directory:
  - terraform
  - cloudformation
framework:
  - terraform
  - cloudformation
download-external-modules: false
skip-path:
  - terraform/.terraform
# Residual checks that stay buyer-optional or would break first-launch.
# Ingress 0.0.0.0/0 is not skipped — it is rejected unless explicitly opted in.
skip-check:
  - CKV_AWS_2 # HTTP listener is first-launch only; HTTPS when a certificate is set
  - CKV_AWS_103 # same HTTP listener
  - CKV2_AWS_20 # HTTP to HTTPS redirect activates when certificate_arn / CertificateArn is set
  - CKV_AWS_157 # Multi-AZ is an opt-in production switch
  - CKV_AWS_293 # RDS deletion protection is an opt-in production switch
  - CKV_AWS_353 # Performance Insights cost on db.t4g.micro
  - CKV_AWS_118 # Enhanced monitoring extra role and cost
  - CKV_AWS_338 # Documented default is 30-day log retention
  - CKV_AWS_336 # Next.js and Hermes need writable tmp; Hermes home is EFS
  - CKV_AWS_150 # ALB deletion protection blocks test teardown
  - CKV2_AWS_62 # No event consumer for uploads or vault objects
  - CKV_AWS_144 # Cross-region replication is buyer-owned DR
  - CKV2_AWS_57 # No Secrets Manager rotation Lambda in this stack
  - CKV_AWS_378 # Target group HTTP is intended; TLS terminates on the ALB
  - CKV2_AWS_50 # Redis Multi-AZ is an opt-in production switch
  - CKV_AWS_161 # App authenticates to Postgres with Secrets Manager, not IAM DB auth
  - CKV2_AWS_30 # postgresql and upgrade logs are already exported
  - CKV_AWS_250 # Hermes s6 must start as root then drop to uid 10000
  - CKV_ECS_8 # same Hermes s6 bootstrap
  - CKV_AWS_382 # Hermes needs SETUID/SETGID/CHOWN after drop-all
